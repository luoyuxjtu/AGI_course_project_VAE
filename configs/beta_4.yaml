exp_name: beta_4
seed: 42
data_dir: data/coco2017
image_size: 256
batch_size: 32
num_workers: 4

latent_dim: 1024
base_channels: 32
beta: 4.0
kl_anneal_epochs: 10

epochs: 100
lr: 0.001
use_amp: true

num_samples: 64
num_interpolations: 8
compute_fid: false
